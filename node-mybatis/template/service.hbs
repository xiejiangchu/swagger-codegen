package {{package}}.service;

import java.util.List;
import {{package}}.bean.{{camelCase tableInfo.tableName}};

/**
 * Created by xie
 */
public interface {{camelCase tableInfo.tableName}}Service {
	{{camelCase tableInfo.tableName}} selectByPrimaryKey(Integer id);
	List<{{camelCase tableInfo.tableName}}> selectByPrimaryKeys(List<Integer> ids);
	int count();
	{{camelCase tableInfo.tableName}} insert({{camelCase tableInfo.tableName}} record);
	{{camelCase tableInfo.tableName}} insertSelective({{camelCase tableInfo.tableName}} record);
	int update({{camelCase tableInfo.tableName}} record);
	int updateByPrimaryKeySelective({{camelCase tableInfo.tableName}} record);
	int updateByPrimaryKey({{camelCase tableInfo.tableName}} record);
	int deleteByPrimaryKey(Integer id);
	int deleteAll();
}