package {{package}}.service;

import java.util.List;
import {{package}}.bean.{{camelCase tableInfo.tableName}};

/**
 * Created by xie
 */
public interface {{camelCase tableInfo.tableName}}Service {
	{{camelCase tableInfo.tableName}} selectById(Integer id);
	List<{{camelCase tableInfo.tableName}}> selectByIds(List<Integer> ids);
	int count();
	{{camelCase tableInfo.tableName}} insert({{camelCase tableInfo.tableName}} record);
	{{camelCase tableInfo.tableName}} insert({{functionParam cols}});
	int update({{camelCase tableInfo.tableName}} record);
	int update({{functionParam cols}});
	int deleteById(Integer id);
	int deleteAll();
}