package {{package}}.mapper;

import java.util.List;
import {{package}}.bean.{{camelCase tableInfo.tableName}};

/**
 * Created by xie
 */
public interface {{camelCase tableInfo.tableName}}Mapper {
	{{camelCase tableInfo.tableName}} getById(Integer id);

	List<{{camelCase tableInfo.tableName}}> getByIds(List<Integer> ids);

	int count();

	int insert({{camelCase tableInfo.tableName}} record);

	int updateById({{camelCase tableInfo.tableName}} record);

	int deleteById(Integer id);
	
	int deleteAll();
}